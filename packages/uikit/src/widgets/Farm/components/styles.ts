import styled from "styled-components";

export const Amount = styled.span<{ amount: number }>`
  color: white;
  display: flex;
  align-items: center;
  font-size: 16px;
  line-height: 160%;
  font-weight: 600;
`;
